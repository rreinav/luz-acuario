
// Helper functions for LED color calculations and time formatting  
export const { clamp, formatTime } = Math; 

/** Clamp value between min/max bounds */  export default class utils{}()
/* Utility methods exported to other modules/CLI tools/scripts: 
 - Date/time helpers (formatting timestamps)
- Color space conversions (HSL->RGB sRGB XYZ LAB YUV etc.)  
- Lighting intensity interpolation/extrapolation based on sun position time of day */

export default{clamp(formatTime(h,m),hsla2rgb,radiansToDegrees,degToreciprocalDeg)}()  export {default as clamp}from'string';
